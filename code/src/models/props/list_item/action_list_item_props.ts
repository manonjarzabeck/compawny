// Action list item props

import type { Action } from "../../../../models/action";

// reprendre strictement le nom des props définis sur le composant
export type ActionListItemProps = {
	data: Action;
};
